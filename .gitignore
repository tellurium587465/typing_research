# 録画・音声（大容量）
*.avi
*.wav
*.mp4
*.task
*.png

# セッション生データ（新構造: sessions/{id}/）
sessions/*/audio.wav
sessions/*/video.avi
sessions/*/pose.json
sessions/*/pose_backup.json

# セッション生データ（旧構造: 後方互換）
session_*_pose.json
session_*_pose_backup.json
session_*_video.*
session_*_audio.*
session_*_keys.json
session_*_integrated.json
session_*_onset_matched.json
session_*_keyboard_grid.json
session_*_meta.json
frame_*.png

# 分析出力（再生成可能）
jasp_trigram_*.csv
jasp_trigram_ALL*.csv
typing_analysis_*.xlsx
output/*.png
output/*.csv

# 仮想環境
__pycache__/
.venv/
.venv310/

# Office ファイル
*.pptx
